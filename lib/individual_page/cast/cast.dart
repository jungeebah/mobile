import 'package:flutter/material.dart';

class CastPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: new Text('Cast Page'),
    );
  }
}
